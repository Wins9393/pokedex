{
  "name": "pokedex-combat",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail",
    "types": "wrangler types && tsc --noEmit"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
